DataStore2.rbxmx
roblox.toml

# Local worker identity — unique per machine, never committed
config/worker-id

# Local AI tool configuration — copy .env.example to .env and fill in values
.env
temp.md
webui/client/dist/
webui/client/node_modules/
webui/server/db/agency.db
webui/__pycache__/

*.pyc

# Game repos — each game lives in its own external git repository
games/*/

# Worktrees — isolated environments for parallel agent work, never committed
.claude/worktrees/

# Generated reports — produced nightly/weekly by agents, not source-controlled
reports/morning/
reports/weekly/

# Runtime logs — generated during agent execution
logs/

# Per-worker heartbeat files and machine-specific worker registry
memory/workers/
memory/workers.md

# Per-game state snapshots — generated by Architect/Planner at runtime
memory/game-states/