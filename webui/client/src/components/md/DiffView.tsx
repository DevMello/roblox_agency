// Stub — full implementation provided by frontend-markdown worker
export function DiffView({ diff }: { diff: string }) {
  return <pre className="font-mono text-xs p-4 overflow-auto text-text-muted">{diff}</pre>
}
