// Stub — full implementation provided by frontend-run-git-components worker
export function BranchBadge({ name }: { name: string }) {
  return (
    <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded bg-surface border border-border text-xs font-mono text-text-muted">
      🌿 {name}
    </span>
  )
}
